{{!-- Activation --}}
<div class="form-group split-group">
    <label>{{ localize label }}</label>
    <div class="form-fields">

        {{!-- Amount --}}
        {{#if activation.scalar}}
        {{ formField fields.value value=data.value label="ME5E.Amount" placeholder="—" localize=true hint=false
                     classes="label-top" rootId=partId }}
        {{/if}}

        {{!-- Type --}}
        {{ formField fields.type value=data.type label="ME5E.Cost" localize=true classes="label-top" hint=false
                     options=activationTypes rootId=partId }}
    </div>

    {{!-- Condition --}}
    {{ formInput fields.condition value=data.condition input=inputs.createTextInput classes="full-width"
                 placeholder=(localize "ME5E.ItemActivationCondition") rootId=partId }}
</div>
