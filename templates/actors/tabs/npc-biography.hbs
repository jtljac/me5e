<section class="tab {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">

    {{#if editingDescription}}
    <prose-mirror name="{{ editingDescription.target }}" value="{{ editingDescription.value }}"
                  document-uuid="{{ actor.uuid }}" compact></prose-mirror>
    {{else}}

    {{!-- Ideals, Bonds, & Flaws --}}
    <div class="top flexrow">

        {{#if (or editable system.details.ideal)}}
        {{~> "me5e.biography-textbox" name="system.details.ideal" value=system.details.ideal label="ME5E.Ideals"
            icon="fas fa-seedling" ~}}
        {{/if}}

        {{#if (or editable system.details.bond)}}
        {{~> "me5e.biography-textbox" name="system.details.bond" value=system.details.bond label="ME5E.Bonds"
            icon="fas fa-link" ~}}
        {{/if}}

        {{#if (or editable system.details.flaw)}}
        {{~> "me5e.biography-textbox" name="system.details.flaw" value=system.details.flaw label="ME5E.Flaws"
            icon="fas fa-heart-crack" ~}}
        {{/if}}

    </div>

    {{!-- Public Information --}}
    <div class="middle" data-target="system.details.biography.public">

        <h3 class="icon">
            <button type="button" class="control-button editor-edit unbutton" data-action="editDescription"
                    data-target="system.details.biography.public"
                    aria-label="{{ localize 'ME5E.BiographyPublicEdit' }}">
                <i class="fas fa-feather" inert></i>
            </button>
            <me5e-icon src="systems/me5e/icons/svg/ink-pot.svg"></me5e-icon>
            <span class="roboto-upper">{{ localize "ME5E.Public" }}</span>
        </h3>

        <div class="editor">
            <div class="editor-content">{{{ enriched.public }}}</div>
        </div>

    </div>

    {{!-- Details --}}
    <div class="bottom" data-target="system.details.biography.value">

        <h3 class="icon">
            <button type="button" class="control-button editor-edit unbutton" data-action="editDescription"
                    data-target="system.details.biography.value" aria-label="{{ localize 'ME5E.DetailsEdit' }}">
                <i class="fas fa-feather" inert></i>
            </button>
            <me5e-icon src="systems/me5e/icons/svg/ink-pot.svg"></me5e-icon>
            <span class="roboto-upper">{{ localize "ME5E.Details" }}</span>
        </h3>

        <div class="editor">
            <div class="editor-content">{{{ enriched.value }}}</div>
        </div>

    </div>

    {{/if}}

</section>
