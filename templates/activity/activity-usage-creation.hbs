<section>
    {{~#if hasCreation}}
    <fieldset>
        <legend>{{ localize "ME5E.USAGE.SECTION.Creation" }}</legend>
        {{#if template}}
            {{ formField template.field name=template.name value=template.value input=inputs.createCheckboxInput }}
        {{/if}}
    </fieldset>
    {{> "me5e.activity-usage-notes" }}
    {{/if~}}
</section>
