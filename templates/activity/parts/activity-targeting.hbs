<section class="tab activity-{{ tab.id }} {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
    <fieldset {{ disabled disabled.range }}>
        <legend>
            <label class="checkbox">
                {{ localize "ME5E.RANGE.FIELDS.range.label" }}
                {{#if (or activity.range.canOverride activity.isRider)}}
                {{ formInput fields.range.fields.override value=source.range.override input=inputs.createCheckboxInput
                             dataset=(me5e-object tooltip="ME5E.RANGE.FIELDS.range.override.hint")
                             ariaLabel=(localize "ME5E.RANGE.FIELDS.range.override.hint")}}
                {{/if}}
            </label>
        </legend>
        {{> "me5e.field-range" range=activity.range fields=fields.range.fields data=data.range rangeTypes=rangeUnits
            inputs=inputs }}
    </fieldset>

    {{> "me5e.field-targets" target=activity.target fields=fields.target.fields data=data.target source=source.target
        inputs=inputs disabled=disabled.target }}
</section>
