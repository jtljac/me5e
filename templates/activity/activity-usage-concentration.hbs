<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "ME5E.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "me5e.activity-usage-notes" }}
    {{/if~}}
</section>
