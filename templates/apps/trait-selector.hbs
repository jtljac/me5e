<form autocomplete="off" onsubmit="event.preventDefault();">
    {{> "me5e.trait-list" prefix="choices"}}

    {{#if customPath}}
        <div class="form-group stacked">
            <label>{{localize "ME5E.TraitSelectorSpecial"}}</label>
            <input type="text" name="{{customPath}}" value="{{custom}}">
        </div>
    {{/if}}

    {{#if bypassesPath}}
        <h3>{{localize "ME5E.DamagePhysicalBypass"}}</h3>
        <p class="note">{{localize "ME5E.DamagePhysicalBypassHint"}}</p>
        {{> "me5e.trait-list" choices=bypasses prefix="bypasses"}}
    {{/if}}

    <button type="submit"><i class="far fa-save"></i> {{localize "ME5E.TraitSave"}}</button>
</form>
